# 引脚分配表（Pin Map & 评审记录）

## 文档控制信息

| 项 | 内容 |
|---|---|
| 项目名称 | 温室环境智能控制节点 |
| 文档编号 | GWN-PIN-004 |
| 版本 | V1.0 |
| 日期 | 2026-07-31 |
| 状态 | **已冻结**（改动需变更评审） |
| MCU | STM32F103RCT6（LQFP64，256KB Flash，48KB RAM） |

---

## 1. 引脚分配总表（冻结版）

### 1.1 通信接口

| 功能 | 引脚 | 复用功能 | 方向 | 备注 |
|---|---|---|---|---|
| RS485_TX | PB10 | USART3_TX | 输出 | 9600bps/8N1 |
| RS485_RX | PB11 | USART3_RX | 输入 | 同上 |
| RS485_DE | PC0 | GPIO输出 | 输出 | 高=发送；TX完成回调翻转 |
| 调试口_TX（开发期） | PA9 | USART1_TX | 输出 | 115200，printf，板载 CH340→USB-C 现成通道 |
| 调试口_RX（开发期） | PA10 | USART1_RX | 输入 | 可选 |
| 调试口_TX（底板期） | PA2 | USART2_TX | 输出 | 115200，printf，排针引出接 USB-TTL |
| 调试口_RX（底板期） | PA3 | USART2_RX | 输入 | 可选 |

### 1.2 执行器与输出

| 功能 | 引脚 | 复用功能 | 说明 |
|---|---|---|---|
| 风机_PWM | PA6 | TIM3_CH1 | 20kHz，PID 输出 |
| 补光_PWM | PA7 | TIM3_CH2 | 20kHz，规则输出 |
| 水泵_继电器 | PB0 | GPIO输出 | 高电平吸合；**上电默认低** |
| 蜂鸣器 | PB4 | GPIO输出 | 高电平鸣叫（需关 JTAG，见 §3） |
| LED_电源 | 板载直连电源灯（3.3V→电阻→LED→GND） | 不占引脚 | 常亮 = 3.3V 供电正常（不依赖 MCU） |
| LED_上联 | PC1 | GPIO输出 | 上联心跳闪烁（高电平有效，外接；工程标签 LED_signal） |
| LED_运行 | PC13 | GPIO输出 | 100ms 周期翻转（高电平有效，外接；工程标签 LED_runing） |

### 1.3 传感器与反馈

| 功能 | 引脚 | 复用功能 | 说明 |
|---|---|---|---|
| 软件I2C_SCL | PB6 | GPIO输出 | AHT20/BH1750/OLED 共用，4.7K 上拉 |
| 软件I2C_SDA | PB7 | GPIO开漏 | 同上 |
| 霍尔测速 | PB1 | EXTI1 | 上升沿中断计数 |
| 土壤湿度_ADC | PA1 | ADC1_IN1 | 0~3.3V 模拟量，12bit |
| 掉电检测_ADC（新增预留） | PC2 | ADC1_IN12 | 12V 分压采样，0~3.3V 模拟量 |

### 1.4 存储与扩展

| 功能 | 引脚 | 复用功能 | 说明 |
|---|---|---|---|
| SD_SCK | PB13 | SPI2_SCK | 400kHz 初始化后提速 |
| SD_MISO | PB14 | SPI2_MISO | 上拉 |
| SD_MOSI | PB15 | SPI2_MOSI | |
| SD_CS | PB12 | GPIO输出 | 低有效 |
| CAN_TX（预留） | PB9 | CAN1_TX | 多节点组网扩展；⚠️ CAN1 默认映射 PA11/12 被板载 USB 占用，必须重映射到 PB8/PB9 |
| CAN_RX（预留） | PB8 | CAN1_RX | 同上 |

### 1.5 按键与调试

| 功能 | 引脚 | 说明 |
|---|---|---|
| KEY1 | PC8 | 翻页（板载 SW2，GPIO输入上拉） |
| KEY2 | PC9 | 自动/手动切换（板载 SW3） |
| 烧录/调试 | PA13/PA14 | SWDIO/SWCLK（ST-Link，保留） |
| BOOT0 | BOOT0 | 启动模式跳线（核心板） |
| NRST | NRST | 复位 |

### 1.6 电源

| 功能 | 引脚 | 说明 |
|---|---|---|
| VDD 3.3V | 全部 VDD | 核心板 AMS1117 输出（底板 5V 供入核心板 5V 引脚） |
| 5V 输入 | 核心板 5V 引脚 | 底板 12V→5V 输出 |
| GND | 全部 GND | 底板共地，RS485/传感器共地 |

---

## 2. 外设冲突检查表（评审记录）

| 检查项 | 结果 |
|---|---|
| USART1（PA9/10）与 USB（PA11/12） | ✅ 不冲突，USB 未用 |
| TIM3_CH1/CH2（PA6/7）与 SPI1（PA5/6/7） | ⚠️ **冲突** → SD 卡改走 SPI2（PB13/14/15），已解决 |
| TIM3（PA6/7）与 ADC 通道（PA6/7 是 ADC1_IN6/7） | ✅ 复用功能不共存，PWM 与 ADC 二选一，本设计 PWM |
| PB3/PB4 与 JTAG | ⚠️ **需在代码中关 JTAG**（见 §3），PB4 作蜂鸣器 |
| PC14/PC15 与 LSE 振荡器 | ⚠️ **需关闭 LSE 功能**（HAL 默认不使能，确认即可），作 LED |
| PA15 与 JTDI | ⚠️ 关 JTAG 后可用作 KEY1 |
| PB6/PB7 与 I2C1 硬件 | ✅ 软件 I2C，仅用 GPIO 功能 |
| EXTI1（PB1）与 ADC1_IN9 | ✅ 复用不冲突 |
| SPI2 与 CAN（PB13/PB14 也是 CAN2_RX/TX） | ⚠️ **CAN2 不可用**（F103 CAN2 依赖 CAN1，无 CAN1 分配）；CAN1 在 PB8/PB9，无冲突 ✅ |
| 定时器需求 | TIM3（PWM 双通道）、TIM4（预留）、SysTick（RTOS）— 资源充足 |
| ADC 通道 | 仅用 ADC1_IN0（PA0），可扩展 IN1~IN7 |

---

## 3. 特殊引脚说明（三大坑）

### 3.1 JTAG 释放（PB3/PB4/PA15）

```c
// 使能 AFIO 时钟后关闭 JTAG，保留 SWD
__HAL_AFIO_REMAP_ENABLE(JTAGDIS);  // 或 RCC_APB2PeriphClockCmd(RCC_APB2Periph_AFIO)
// 关闭后：PB3/PB4/PA15 释放为 GPIO，SWD(PA13/14) 不受影响
```

### 3.2 LSE 释放（PC14/PC15）

默认 HAL 工程未启用 LSE，PC14/PC15 可直接作 GPIO。**若使用 CubeMX 默认勾选了 LSE 需取消。**

### 3.3 BOOT0 与下载

核心板 BOOT0 跳线：开发阶段 BOOT0=0（从 Flash 启动）；若需串口 ISP 下载临时拉高。SWD 下载不受影响。

---

## 4. 底板排针定义（核心板接口约定）

底板排母按核心板双排 2.54mm 引脚顺序布局，与主控相关的引出：

| 排针分组 | 引出引脚 |
|---|---|
| 电源组 | 12V_IN、5V、3.3V、GND×3 |
| 485 组 | PA9、PA10、PA8（DE）、GND |
| 执行器组 | PA6（风机PWM）、PA7（补光PWM）、PB0（水泵）、PB4（蜂鸣器） |
| 传感器组 | PB6(SCL)、PB7(SDA)、3.3V、GND（I2C 三设备共用母线） |
| ADC 组 | PA0、3.3V、GND |
| 反馈组 | PB1（霍尔）、3.3V、GND |
| SD 组 | PB13/14/15、PB12、3.3V、GND |
| 扩展组 | PB8/PB9（CAN 预留）、KEY1/KEY2（PA15/PB5）、USART2（PA2/PA3） |

> 底板设计说明见《08-硬件设计说明.md》；**此表冻结后，底板 Layout 引脚一律以本表为准，改引脚须走变更评审。**

---

## 5. 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|---|---|---|---|
| V1.0 | 2026-07-31 | 首次冻结 |
| V1.1 | 2026-08-06 | 实物适配变更（绿深系统板）：RS485→USART3(PB10/11)、DE→PC0、土壤ADC→PA1、掉电检测→PC2(新增)、LED→PA8(板载低有效)/PC13/PC1、KEY→PC8/PC9(板载)。原因:板载 CH340 占 PA9/10、WK_UP 占 PA0、USB 占 PA11/12、RTC 晶振占 PC14/15、LED2 占 PA8 | 郑权 |
| V1.1.1 | 2026-08-06 | LED 命名统一为工程标签:上联=PC1(LED_signal)、运行=PC13(LED_runing);LED_电源改用板载直连电源灯(不占引脚),PA8 板载 LED2 闲置 | 郑权 |
| V1.1.2 | 2026-08-06 | 开发期调试口改用 USART1(PA9/10,板载 CH340 现成通道);USART2(PA2/3)保留为底板期调试口 | 郑权 |
